name: Build Openlist-beta and Release

on:
  workflow_dispatch:
    inputs:
      publish:
        description: '是否发布到 Fndepot Release'
        required: false
        type: boolean
        default: true
  schedule:
    - cron: '0 0 * * 6'   # 每周六 0:00 UTC

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-24.04
            platform: x86
            arch: amd64
          - os: ubuntu-24.04-arm
            platform: arm
            arch: arm64
    runs-on: ${{ matrix.os }}
    outputs:
      app_name: ${{ steps.set_vars.outputs.app_name }}
      manifest_version: ${{ steps.set_vars.outputs.manifest_version }}
      changelog: ${{ steps.set_vars.outputs.changelog }}
    steps:
      - name: 生成版本号（当天日期）
        id: version
        run: |
          VERSION=$(date +'%Y.%-m.%-d')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "版本号: $VERSION"

      - name: 设置项目变量
        id: set_vars
        run: |
          APP_NAME="Openlist-beta"
          MANIFEST_VERSION="${{ steps.version.outputs.version }}"
          CHANGELOG="更新测试版本"
          echo "app_name=$APP_NAME" >> $GITHUB_OUTPUT
          echo "manifest_version=$MANIFEST_VERSION" >> $GITHUB_OUTPUT
          echo "changelog=$CHANGELOG" >> $GITHUB_OUTPUT

      - name: 检出 buildbot 仓库（包含模板）
        uses: actions/checkout@v7
        with:
          path: buildbot_repo

      - name: 下载并解压 Openlist 二进制
        run: |
          ARCH="${{ matrix.arch }}"
          DOWNLOAD_URL="https://github.com/OpenListTeam/OpenList/releases/download/beta/openlist-linux-${ARCH}.tar.gz"
          wget "$DOWNLOAD_URL" -O openlist.tar.gz
          tar -xzf openlist.tar.gz
          ls -la
          if [ ! -f openlist ]; then
            echo "错误：未找到 openlist 二进制文件"
            exit 1
          fi
          chmod +x openlist

      - name: 准备 fnpack 工作区
        run: |
          mkdir -p build_workspace
          cp -r "buildbot_repo/fnpack/${{ steps.set_vars.outputs.app_name }}" build_workspace/
          if [ "${{ matrix.platform }}" = "x86" ]; then
            FNPACK_BIN="fnpack-1.2.1-linux-amd64"
          else
            FNPACK_BIN="fnpack-1.2.1-linux-arm64"
          fi
          cp "buildbot_repo/fnpack/$FNPACK_BIN" build_workspace/fnpack
          chmod +x build_workspace/fnpack

      - name: 复制产物到 app/bin
        run: |
          mkdir -p build_workspace/Openlist-beta/app/bin
          cp openlist build_workspace/Openlist-beta/app/bin/
          chmod +x build_workspace/Openlist-beta/app/bin/openlist

      - name: 修改 manifest 文件
        run: |
          MANIFEST_PATH="build_workspace/${{ steps.set_vars.outputs.app_name }}/manifest"
          if [ ! -f "$MANIFEST_PATH" ]; then
            echo "错误：manifest 文件不存在于 $MANIFEST_PATH"
            exit 1
          fi
          MANIFEST_VERSION="${{ steps.set_vars.outputs.manifest_version }}"
          CHANGELOG="${{ steps.set_vars.outputs.changelog }}"
          ESCAPED_CHANGELOG=$(printf '%s' "$CHANGELOG" | jq -sRr @json)
          sed -i "s/\${VERSION}/$MANIFEST_VERSION/g" "$MANIFEST_PATH"
          sed -i "s/\${CHANGELOG}/$ESCAPED_CHANGELOG/g" "$MANIFEST_PATH"
          sed -i "s/\${PLATFORM}/${{ matrix.platform }}/g" "$MANIFEST_PATH"
          echo "manifest 修改后的内容："
          cat "$MANIFEST_PATH"

      - name: 执行 fnpack 打包
        run: |
          cd build_workspace
          ./fnpack build "${{ steps.set_vars.outputs.app_name }}"
          FPK_FILE=$(ls *.fpk | head -n 1)
          if [ -z "$FPK_FILE" ]; then
            echo "错误：未找到生成的 fpk 文件"
            exit 1
          fi
          FINAL_NAME="${{ steps.set_vars.outputs.app_name }}-${{ steps.set_vars.outputs.manifest_version }}-${{ matrix.platform }}.fpk"
          mv "$FPK_FILE" "$FINAL_NAME"
          echo "FINAL_FPK=$FINAL_NAME" >> $GITHUB_ENV
          echo "打包完成，生成文件：$FINAL_NAME"
          ls -la *.fpk

      - name: 上传 fpk 文件作为 Artifact
        uses: actions/upload-artifact@v7
        with:
          name: fpk-${{ matrix.platform }}
          path: build_workspace/*.fpk
          if-no-files-found: error

  # ========== 发布到 Fndepot ==========
  publish:
    name: 发布到 Fndepot Release
    needs: build
    if: always() && needs.build.result == 'success' && (github.event_name != 'workflow_dispatch' || github.event.inputs.publish == 'true')
    uses: ./.github/workflows/Publish-x86-arm.yaml
    with:
      app_name: ${{ needs.build.outputs.app_name }}
      manifest_version: ${{ needs.build.outputs.manifest_version }}
      changelog: ${{ needs.build.outputs.changelog }}
      artifact_pattern: 'fpk-*'
    secrets:
      BUILDBOT: ${{ secrets.BUILDBOT }}
